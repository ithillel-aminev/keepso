{% extends '::base.html.twig' %}

{% block body %}
    {% if app.user %}
        {{ include('BlogBundle:includes:dashboard.html.twig') }}
    {% endif %}
    {{ form_start(form) }}
    {{ form_widget(form) }}
    {{ form_end(form) }}
{% endblock %}
